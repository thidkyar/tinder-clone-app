import React from 'react'
import { View, Text } from 'react-native'

const MatchedScreen = ({loggedInProfile, userSwiped}) => {
    return (
        <View>
            <Text>MATCHED</Text>
        </View>
    )
}

export default MatchedScreen
